# One Frame Live Interpreter

A simple command line application that acts as a live interpreter to the One Frame API Service.

## To Run

Run the Main class and then interact with the service via the command line.